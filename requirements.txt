python-telegram-bot==21.10
stcp-api==2.0.3
aiohttp==3.11.11
python-dotenv==1.0.1
aiofiles==24.1.0
asyncpg==0.30.0
flask>=3.0.0
requests>=2.31.0
# Station data is now fetched via aiohttp from Metro do Porto JS files.
# Playwright/Chromium is no longer needed (the trip planner uses a
# referrer-restricted Google Maps API key that cannot be called server-side,
# and Chromium does not work through corporate/egress proxies).
